package admin

import (
	"yumi/model"
)

func Login(params model.Login) (model.Staff, error) {
	return model.Staff{}, nil
}

func Logout(params model.Login) (model.Staff, error) {
	return model.Staff{}, nil
}
